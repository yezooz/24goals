from django.conf.urls.defaults import *
from api.views import *

urlpatterns = patterns('myscore.api',
                       # (r'^igoogle/', include('igoogle.urls')),

                       # (r'^get_news$', 'interface.views.get_news'),
                       )
